package com.abiratsis.gweather.config

case class Settings(app: ApplicationSettings, user: UserSettings)

object Settings{
//  def apply(): Settings = {
////    new Settings(ApplicationSettings, UserSettings())
//  }
}

