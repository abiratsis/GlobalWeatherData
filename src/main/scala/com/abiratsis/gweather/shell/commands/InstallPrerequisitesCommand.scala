package com.abiratsis.gweather.shell.commands

/**
 * Handles the install_prerequisites function in the shell script.
 */
class InstallPrerequisitesCommand extends ShellCommand {
  /**
   * The function name in the shell script.
   *
   * @return
   */
  override protected def functionName: String = "install_prerequisites"
}
