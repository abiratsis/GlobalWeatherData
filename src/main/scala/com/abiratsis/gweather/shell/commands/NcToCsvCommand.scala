package com.abiratsis.gweather.shell.commands

/**
 * Handles the .nc to .csv conversion of the data.
 */
private[gweather] class NcToCsvCommand extends ShellCommand {
  override protected def functionName: String = "nc_to_csv"
}